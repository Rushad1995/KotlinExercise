import io.kotest.core.spec.style.StringSpec
import io.kotest.matchers.shouldBe
import org.junit.jupiter.api.Assertions.*

internal class FatherTest : StringSpec({
    var sun = Sun()
    "Inheritance example "{
        sun.name("Sunny","Patil") shouldBe
    }
})